<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Base TextInputLayout style-->
    <style name="TextInputLayout_Style" parent="Widget.Design.TextInputLayout">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">60dp</item>
        <!--<item name="layout_alignParentStart">true</item>-->
    </style>

    <!-- Base button style -->
    <style name="AppButton_Style" parent="Base.Widget.AppCompat.Button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:gravity">center</item>
        <item name="android:backgroundTint">@color/button_Color</item>
        <item name="android:textSize">15sp</item>
    </style>

    <!-- Base EditText style -->
    <style name="EditText_Style" parent="Base.Widget.AppCompat.EditText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">50dp</item>
       <!-- <item name="android:layout_alignParentStart">true</item>-->
        <!--<item name="textColor">@color/text_color</item>-->
        <item name="android:inputType">numberDecimal</item>
    </style>

    <!-- Base TextView style -->
    <style name="TextView_Style" parent="Widget.AppCompat.AutoCompleteTextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:textColor">@color/text_Color</item>
    </style>

</resources>
